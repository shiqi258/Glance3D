# Ralph Progress Log
Started: Fri, Jul 10, 2026  6:13:05 AM
---

## Codebase Patterns（先读——最重要的可复用经验，随迭代增补）

### 构建预设 / 二进制路径
- **桌面 Release**：`cmake --build --preset native-local` → 产物 `build/bin_Release/`。
- **严格 Debug（dev-strict）**：本机用 `dev-local` 预设（在 `CMakeUserPresets.json`，inherits `dev`+`native-local`，binaryDir=`build-dev`）。配置 `cmake --preset dev-local`，构建 `cmake --build --preset dev-local`（或单目标 `cmake --build build-dev --target <t> --config Debug`）。`dev` 预设本身没有 VTK_DIR/binaryDir，别直接用。
- **US-001 起，桌面二进制名为 `glance3d.exe`**（原 `f3d.exe`）。CMake 目标名仍是 `f3d`/`f3d-console`（`$<TARGET_FILE:f3d>`、`f3dTargets` 导出零波及），只在 `application/CMakeLists.txt` 用 `set_target_properties(... OUTPUT_NAME glance3d)` 改产物名。库 DLL 仍是 `f3d.dll`（US-001 不动库）。

### 运行时路径解析与更名无关（重要）
- 资源锚点走 `GetApplicationPath()`（Win=`GetModuleFileNameW(nullptr,...)`，Linux=`/proc/self/exe`）→ 与 exe **文件名无关**，只看真实模块路径；`GetBinaryResourceDirectory()` 取 `<exe>/../../share/f3d`。更名 exe 不破坏资源解析（已实证：glance3d.exe 出图、字体/UI/网格全部正常）。
- 全仓唯一用 `argv[0]` 的地方：`application/F3DOptionsTools.cxx:277`——仅用于 cxxopts 的 **帮助/Usage 显示名**，非路径。故更名后 `--help` 的 Usage 行自动变 `glance3d.exe`，无需改代码。

### Windows 版本资源（VERSIONINFO）注入范式（US-002 起）
- 编译进 exe 的资源文件是 `resources/f3d.rc`（在 `application/CMakeLists.txt:220` 通过 `list(APPEND F3D_SOURCE_FILES ...f3d.rc)` 加入，仅 WIN32）。第一行仍是 `IDI_ICON1 ICON DISCARDABLE "logo.ico"`（须保持在最前，Explorer/任务栏取首个图标）。
- **版本号不写死**：`resources/Glance3DVersion.h.in`（`@F3D_MAJOR_VERSION@` 等占位）经 `configure_file(... @ONLY)` 生成到 `${CMAKE_CURRENT_BINARY_DIR}/Glance3DVersion.h`（即 `build*/application/`，与 `F3DConfig.h` 同目录），`f3d.rc` 用 `#include "Glance3DVersion.h"` 取宏。configure_file 放在 application/CMakeLists.txt 的 `F3DConfig.h` 配置之后、`WIN32` 守卫内。
- **RC 能找到生成头**：靠 `f3d_create_exec()` 里的 `target_include_directories(${target} PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_BINARY_DIR}>)`——VS 生成器会把 target 的 include 目录传给资源编译器（ResourceCompile），无需给 .rc 单独设 include。已实证 Release/Debug 两树均生成头且 RC 编译通过。
- 改了 configure_file/模板后**必须重跑 `cmake --preset <preset>` 再 build**（configure_file 在配置期执行）。native-local 与 dev-local 两树各自需重配。
- 验证：`(Get-Item build/bin_Release/glance3d.exe).VersionInfo` 读 ProductName/FileDescription/OriginalFilename/FileVersion 等字段。`.rc` 由 rc.exe 编译，**不受 C++ `/W4 /WX`（F3D_STRICT_BUILD）约束**，故纯资源改动不会引入新 strict 告警。

### 哪些目标吃 `F3D_STRICT_BUILD`（/W4 /WX）——决定源码型 story 的验证范围
`f3d_compile_options_private`（= MSVC `/W4 /WX`，见顶层 `CMakeLists.txt:150,200`）**只施加于**这些目标：`vtkext`（public/private，`vtk_module_compile_options`）、`libf3d`（`library/CMakeLists.txt:181`）、`library` 测试、`application`（`f3d_create_exec` 内 `application/CMakeLists.txt:163`）、各插件（`cmake/f3dPlugin.cmake:272`）。**不施加于** `winshellext`（F3DShellExtension，其 CMakeLists 未引用该变量）。所以：改 `winshellext/*` 天然不受 strict 约束（Release 编过即可）；改 `library/`、`application/`、`vtkext/` 才需按“源码型 story 实操建议”核对无新增 strict 告警。

### 更名后的 Linux 桌面集成命名自洽（US-003 起）
更名波及一组**必须彼此一致**的名字：`resources/glance3d.desktop` 的 `Exec/TryExec=glance3d` ↔ 安装的二进制名（US-001 `OUTPUT_NAME glance3d`）；`Icon=glance3d` ↔ `application/CMakeLists.txt` 图标 `install(... RENAME "glance3d.png"/"glance3d.svg")`；bash 补全 `RENAME "glance3d"` 必须等于命令名 `glance3d`（zsh `_glance3d`、fish `glance3d.fish`）；man `MAN_OUTPUT_FILE=…/glance3d.1` 与其 `.gz` install 两处同改。改任一处务必同步其余，否则装机后图标/补全/桌面项失效。winshellext 缩略图 DLL 按**同目录字面量** `L"glance3d.exe"` 定位渲染进程（`F3DThumbnailProvider.cxx:103`），也须与产物名一致。

### 文档更名 story 的范围与 CLAUDE.md gitignore（US-004 起）
- **CLAUDE.md 被 `.gitignore:43` 忽略**（本机工作文件，非 tracked）。对它的编辑在磁盘上生效、但**不进 commit**，也不出现在 `git diff`/`git status`。故涉及 CLAUDE.md 的验收靠“内容更新 + 实测命令可跑”而非 diff；`git diff --stat` 看不到它是正常的。
- **`f3d.exe` vs 裸命令名 `f3d`**：更名 story（US-001/003/004）只处理**可执行文件名** `f3d.exe`/`f3d-console.exe`→`glance3d.exe`/`glance3d-console.exe`。文档里大量**裸命令名 `f3d`**（如 `f3d --version`、`f3d --config`）指“安装后键入的命令”，虽然 US-001 `OUTPUT_NAME=glance3d` 后该命令实际是 `glance3d`，但把裸 `f3d`→`glance3d` 全量替换是**另一个更大范围的改造，当前无 story 覆盖**，且易误伤上游项目名 `F3D`/`f3d` 语境。别在更名 story 里顺手做——只在同一句同时出现二者时（如 `f3d --version` or `f3d-console.exe --version`）注意别造半更名的不一致句，宁可整句留待专门 story。
- **过程/需求文档保留旧名是预期的**：`ralph/*`（prd.json/progress.txt/ralph-prompt.md）、`tasks/*`（需求 PRD）在描述“本次更名任务”时**必须引用旧名** `f3d.exe`，性质等同历史 changelog。故 `rg 'f3d\.exe'`/`rg 'bin_Release/f3d\.exe'` 的“全仓命中 0”应理解为**功能性代码/用户文档命中 0**，process 文档命中不算违规（US-003/US-004 均按此口径验收）。

### ⚠️ dev-strict 严格构建在本机存在“先天”阻断（非某一 story 引入，务必知悉）
`F3D_STRICT_BUILD` = MSVC `/W4 /WX`（warning 即 error）。本机 `dev-local` 全绿构建当前**不可达**，原因有二，均与 US-001 更名无关：
1. **库源码告警被当错误**（Glance3D 既有代码，pre-existing）：
   - `library/public/scene.h` 的 `g3d_scene_tree_node / g3d_scene_tree_snapshot / g3d_data_array_info / g3d_data_info` 都 `F3D_EXPORT` 且含 `std::string/std::vector` 数据成员 → **C4251**（dll-interface）→ C2220。根因：上游惯例是**纯数据 DTO 不加 `F3D_EXPORT`**（见 `mesh_view` 的 `data_array_t/memory_view_t`——外层类导出、内层数据结构体不导出）。**建议修法**：去掉这 4 个 DTO 的 `F3D_EXPORT`（枚举 `g3d_scene_tree_node_kind`、全 POD 的 `g3d_scene_tree_capabilities` 无 C4251，可留）。已验证该改动能消除 scene.h 的 C4251。
   - `library/src/scene_impl.cxx:~931` 的 `return nullptr;` → **C4702**（不可达代码，`if constexpr` 整型分支已 return）→ C2220。属上游 mesh-view→vtk 转换代码，编译器版本相关。建议按分支加 `if constexpr` 守卫或局部 `#pragma warning(suppress:4702)`。
   - （上面两个只是当前暴露到的前两层；strict 全绿可能还有后续被掩盖的告警，需逐个消。）
2. **环境性链接冲突**（依赖问题，本机无法自修）：本机 `VTK_DIR` 是 **Release-only** VTK，`dev` 是 Debug（MDd）。测试可执行 `vtkextTests` 直接链 VTK 静态工具库（vtksys/vtkfmt/vtktoken…）→ **LNK2038**（`RuntimeLibrary` MD_DynamicRelease vs MDd_DynamicDebug、`_ITERATOR_DEBUG_LEVEL` 0 vs 2）→ LNK1218。要修需一份 `VTK_WEBASSEMBLY`… 不，需一份 **Debug 构建的 VTK**。此项非代码问题。

**给源码型 story 的实操建议（US-005/006/007/008/009 必看）**：全绿 dev-local 在本机不可达，故对“改了源码”的 story，验证策略 = 用 `cmake --build build-dev --target f3d --config Debug` 确认**改动目标（app+libf3d）严格编译零新增告警**（对比基线已知的 pre-existing 告警清单，只要不新增即可），而非追求整棵测试树全绿。若要真正解锁 strict 门禁，需先专门治理上面第 1 条的库告警债（独立一轮，别塞进功能 story）。

### ✅ 隔离式 strict 验证：单文件 `/W4 /WX` 编译（US-009 起，改 application 源码的 story 复用）
`cmake --build build-dev --target f3d` 会先编 libf3d，撞上上面的 pre-existing scene.h C4251 债直接停，**永远到不了 application 源码**，无法证明“我改的 app 文件 strict 干净”。解法 = 用 MSBuild **单文件 ClCompile**，只编改动的 `.cxx`、不建 libf3d：
1. 找 MSBuild：`vswhere -latest -find MSBuild\**\Bin\MSBuild.exe`（本机 `D:\VS2022\IDE\MSBuild\Current\Bin\MSBuild.exe`）。
2. 编单文件（用 build-dev 的 app vcxproj，带 /W4 /WX）：
   `& $msbuild build-dev\application\f3d.vcxproj /t:ClCompile "/p:Configuration=Debug" "/p:SelectedFiles=<abs .cxx>" "/p:BuildProjectReferences=false"`
   - **一次只传一个文件**（多个用 `;` 分隔会被 MSBuild 当成开关炸掉）。EXITCODE=0 且无 `warning C####` = 该文件 strict 干净。
3. **缺生成头**：app 源码 include 的 `F3DIcon.h`（图标字节数组）在 build-dev 里没生成（全量 app 从没编过）→ C1083。从 Release 树拷过去救急：`cp build/application/F3DIcon.h build-dev/application/`（验完删）。`F3DConfig.h`/`Glance3DVersion.h` 在 build-dev/application 已有。
4. **文件若 include libf3d public 头**（如 `F3DStarter.cxx`→scene.h）会连带报 pre-existing scene.h C4251——不是你的告警。要隔离出“纯你的”告警，**临时**去掉 scene.h 4 个 DTO 的 `F3D_EXPORT`（`g3d_scene_tree_node/_snapshot/g3d_data_array_info/g3d_data_info`，见上方库债段），编完 `git checkout -- library/public/scene.h` 还原。只 include F3DSystemTools.h 之类的文件（如 `F3DSystemTools.cxx`）不碰 scene.h，无需这步。
- 别真去把 libf3d strict 编绿——scene_impl.cxx:931 的 C4702 是 template-lambda 实例化点告警，`#pragma warning(push/disable/pop)` 包不住（告警在实例化点而非定义点报），是独立库债治理，别塞进功能 story。

### 用户配置目录品牌隔离 + 迁移（US-009 起）
- **单点改名 + 迁移都在** `F3DSystemTools::GetUserConfigFileDirectory()`（application/F3DSystemTools.cxx）：目录名 `f3d`→`Glance3D`，且首调时**一次性**把旧 `…\f3d` 递归拷到新 `…\Glance3D`（从不移动/删除旧目录——它仍是官方 F3D 的活动目录）。用**函数内 `static const` memoize**，迁移+原因码日志**每进程只跑一次**；colormaps 等派生路径（`F3DColorMapTools::Find` 用 `GetUserConfigFileDirectory()/"colormaps"`）自动随之切换。
- 决策日志原因码（都带 `config-dir:` 前缀，file log 恒抓 DEBUG）：`fresh`(都不存在，不建目录) | `existing`(新目录在，直接用、绝不读旧) | `migrated(<n> files)`(n 只数普通文件) | `migrate-failed-fallback`(WARN，清残缺新目录+本次回退旧目录+下次重试)。用 `fs::is_directory`（非 `exists`）判定，杂散非目录文件不当作已建目录。硬失败 = 建目录失败/`no_space_on_device`（throw→清理+fallback）；单个不可读文件 = skip+逐条 WARN（不中止整体）。
- **`--no-config` 天然免疫迁移**：`ReadConfigFiles` 被 `!noConfig` 网关，不调该 getter；除非用了 colormap 才会触发（正确行为）。
- **缓存隔离走应用层覆盖**：`F3DStarter` 引擎创建后（NoRender+render 两支都覆盖）`engine.setCachePath(GetUserCacheDirectory()/"cache")` → `%LOCALAPPDATA%\Glance3D\cache`；**不动** `library/src/engine.cxx:95` 的 libf3d 默认（`%LOCALAPPDATA%\f3d`，SDK/wasm 消费者零影响）。`window_impl::SetCachePath` 会 `create_directories` 且可 throw `cache_exception`——须 try/catch，失败仅 WARN 不致命。
- **端到端验证只能用真 `%APPDATA%`**：`getKnownFolder(ROAMINGAPPDATA)` 走 `SHGetKnownFolderPath`（utils.cxx:375），**不认 `APPDATA` 环境变量重定向**。测法 = 真目录放唯一标记文件（+colormaps 子目录标记）→启动→断言新目录出现拷贝/旧目录原样/日志原因码，**事后清理**。`migrate-failed-fallback` 可注入：先删新目录、在 `%APPDATA%\Glance3D` 放一个**普通文件**（非目录）→ is_directory=false 触发迁移→`create_directories` 撞文件抛错→走 fallback+清理（`remove_all` 删掉那个文件）。

### 共享核心窗口几何读取 API（US-005 起）
- **`window` 唯一具体子类是 `window_impl`**（`library/private/window_impl.h`）——给 `library/public/window.h` 加纯虚 API 只需在 `window_impl` 实现一处，桌面 + wasm 两端自动受益。additive、非破坏，js/python/java 绑定不强制暴露（不动绑定即可）。
- **读窗口位置** `getPosition()` 返回 `std::array<int,2> {x,y}`（与 `setPosition` 同一 top-left 屏幕坐标约定）：`RenWin->GetPosition()` 直读；**Cocoa 分支须镜像 `setPosition` 的 Y 翻转**（`screenSize[1]-winSize[1]-pos[1]`）才 round-trip。offscreen/mock(NONE)/WASM 无真实屏幕位置 → 返回 last-set（vtkRenderWindow 存 ivar）或 `{0,0}`，不抛。
- **读最大化** `isMaximized()`：`#ifdef _WIN32` 下 `static_cast<HWND>(RenWin->GetGenericWindowId())`（与 `G3DScreenSampler.cxx`/`vtkF3DImguiActor.cxx` 同一取 HWND 模式）→ `GetWindowPlacement` → `showCmd == static_cast<UINT>(SW_SHOWMAXIMIZED)`。**cast SW_SHOWMAXIMIZED 到 UINT** 否则 /W4 报 C4389 signed/unsigned 比较→/WX error。HWND 空/查询失败/非 Windows 一律 `false`（设计降级）。`windows.h` 在 `_WIN32` 下经 `vtkF3DWGLRenderWindow.h → vtkWin32OpenGLRenderWindow.h` 传递可见，无需显式 include（本 TU 无 std::min/max，NOMINMAX 非问题）。
- **round-trip 运行时证据**：真窗（`--interaction-test-play` 且**不带** `--output`/`--reference` → `offscreen=false`）+ `--position X,Y`，`ApplyPositionAndResolution` 应用后读回 `getPosition()` 写 DEBUG 日志；PowerShell P/Invoke `GetWindowRect`/`IsZoomed` 三方对齐。`--interaction-test-play` 无 `--output` 时**不自动退出**（窗口停在 interactor 循环，可趁机 P/Invoke 抓 rect，再 `Stop-Process`）；带 `--output`/`--reference` 才 offscreen+截图+退出（ctest 用法）。

### wasm 构建：worktree 干净 shell 里 emsdk 自激活失效（US-005 复现）
- `npm run build`（`webassembly/build-local.mjs`）本应自动激活 emsdk，但**本机干净 shell 下 `emsdk_env.bat` 激活失效 → `emcmake` 找不到**，且脚本用 `| tail`/`| Select-Object` 时 exit code 来自管道尾、**npm 失败被 exit 0 掩盖**（务必读 build 输出确认，别只看退出码）。修法 = 显式设三环境变量后再 `npm run build`（脚本见 `process.env.EMSDK` 已设即跳过自激活、只补 PATH）：
  - `EMSDK=G:\dependency\emsdk`、`EM_CONFIG=G:\dependency\emsdk\.emscripten`
  - `PATH` 前置 `…\emsdk\upstream\emscripten`（emcmake/emcc）+ `…\emsdk\python\<ver>`（python）+ `…\emsdk\node\<ver>\bin`（node）+ `G:\dependency\ninja`
  - PowerShell 每次调用是新 session，**须与 `npm run build` 同一次调用内设置**（`$env:` 不跨调用持久）。
- wasm 默认 threads 版（产物 `_wasm_build_threads/`），配 `F3D_STRICT_BUILD=ON` → **改共享核心的 story 顺带拿到 clang 端 strict 校验**。成功标志：build 日志见改动文件 `.cxx.o` 重编 + `Linking CXX executable bin\f3d.js` + copy 到 `dist/`；`pthread + ALLOW_MEMORY_GROWTH` em++ 告警是 pre-existing 无关噪声。

---

## 2026-07-10 06:35 - US-001 构建产物更名为 glance3d
- **实现**：`application/CMakeLists.txt` 对 `f3d` 与 `f3d-console` 目标各加一行 `set_target_properties(... PROPERTIES OUTPUT_NAME glance3d / glance3d-console)`，只改产物文件名，保持 CMake 目标名与 `f3dTargets` 导出不变。（本轮开始时该改动已在工作区未提交，本轮补完验证并落定。）
- **文件变更**：`application/CMakeLists.txt`（+7 行注释与两处 OUTPUT_NAME）。
- **验证证据（逐条对齐 acceptanceCriteria）**：
  1. 目标名不变、仅 OUTPUT_NAME=glance3d/glance3d-console ✓（f3d-console 目标本预设未启用，代码路径受 `F3D_WINDOWS_BUILD_CONSOLE_APPLICATION` 守卫，改动正确但未由 native-local 触发）。
  2. `cmake --build --preset native-local` 通过，产物 `build/bin_Release/glance3d.exe` ✓（构建日志 `f3d.vcxproj -> ...\glance3d.exe`）。
  3. 无旧 `build/bin_Release/f3d.exe` 残留 ✓（`ls` 确认 No such file；重建即替换）。
  4. `glance3d.exe testing/data/f3d.glb --output shot.png --resolution 800,600 -x -g` 出图成功，Read 读图：Glance3D logo 模型 + 地面网格 + 坐标轴 gizmo + UI 汉堡按钮全部正常 → 资源按目录锚点解析、不受文件名影响，实证通过 ✓。
  5. argv[0]/exe 文件名路径推导全局排查 ✓：唯一 argv[0] 用处是 `F3DOptionsTools.cxx:277` 的帮助显示名（非路径）；路径解析全走 `GetModuleFileNameW`/`GetApplicationPath`（文件名无关）。附带实证 `--help` 的 Usage 行已自动显示 `glance3d.exe`。
  6. native-local 构建通过 ✓。**dev-strict（F3D_STRICT_BUILD）全绿不可达**——US-001 未改任何 C++ 源码，不可能引入新的 strict 告警；dev-local 全绿被 pre-existing 库告警债（scene.h C4251、scene_impl.cxx C4702…）与环境性 Release-VTK/Debug-CRT 测试链接冲突（LNK2038）阻断，二者均与更名无关（详见上方 Codebase Patterns ⚠️ 段）。
- **取舍**：中途曾试探性修 scene.h 的 C4251（去 F3D_EXPORT），确认能消除该告警；但发现 strict 全绿还需连带修 scene_impl.cxx C4702 等更多 pre-existing 库告警 + 一份 Debug VTK，属独立的库告警债治理，超出“更名”story 范围 → **已回退 scene.h**，保持 US-001 纯更名、聚焦最小。库告警债与建议修法已完整记入 Codebase Patterns，留给源码型 story（US-005+）前置处理。
- **Learnings for future iterations：**
  - 本机严格构建预设是 `dev-local`（build-dev/），不是裸 `dev`；`dev` 缺 VTK_DIR/binaryDir。
  - 更名 exe 对运行时零影响，因资源锚点用真实模块路径而非 argv[0]/文件名。
  - 别在功能 story 里顺手修 strict 库告警——那是一整轮独立的技术债，且本机还缺 Debug VTK，全绿本就不可达；按 Codebase Patterns 的“源码型 story 实操建议”验证增量即可。
---

## 2026-07-10 06:48 - US-002 Windows 版本信息资源（VERSIONINFO）
- **实现**：给 exe 加 Windows 版本信息资源，任务管理器/文件属性里显示 Glance3D 产品名与版本号。
  - 新增 `resources/Glance3DVersion.h.in`：configure_file 模板，`@F3D_MAJOR_VERSION@ / @F3D_MINOR_VERSION@ / @F3D_PATCH_VERSION@` 及 `"@F3D_VERSION@"` 占位 → 版本单一来源自顶层 CMakeLists 的 F3D_VERSION，不写死。
  - `resources/f3d.rc`：保留首行图标；新增 `#include <windows.h>` + `#include "Glance3DVersion.h"`，补 `VS_VERSION_INFO VERSIONINFO` 块。FILEVERSION/PRODUCTVERSION 用宏 `GLANCE3D_VERSION_MAJOR,MINOR,PATCH,0`；StringFileInfo 040904b0：ProductName=Glance3D、FileDescription=Glance3D - A fast and minimalist 3D viewer、OriginalFilename=glance3d.exe、FileVersion/ProductVersion=GLANCE3D_VERSION_STR、另附 CompanyName/InternalName/LegalCopyright；VarFileInfo Translation 0x409,1200。
  - `application/CMakeLists.txt`：在 `F3DConfig.h` 的 configure_file 之后加 `if(WIN32)` 守卫的 `configure_file(resources/Glance3DVersion.h.in → ${CMAKE_CURRENT_BINARY_DIR}/Glance3DVersion.h @ONLY)`。
- **文件变更**：`resources/Glance3DVersion.h.in`（新增）、`resources/f3d.rc`、`application/CMakeLists.txt`。
- **验证证据（逐条对齐 acceptanceCriteria）**：
  1. f3d.rc 补 VERSIONINFO 块，ProductName/FileDescription/OriginalFilename 均按要求 ✓。
  2. FileVersion/ProductVersion 取自 F3D_VERSION，经 configure_file 生成头注入、无写死 ✓（生成头 build/application/Glance3DVersion.h 内容为 3.5.0）。
  3. `(Get-Item build/bin_Release/glance3d.exe).VersionInfo` 实测：ProductName=Glance3D、FileDescription=Glance3D - A fast and minimalist 3D viewer、OriginalFilename=glance3d.exe、CompanyName=Glance3D、LegalCopyright=Copyright (C) Glance3D contributors、InternalName=glance3d、FileVersion=3.5.0、ProductVersion=3.5.0、FileVersionRaw=3.5.0.0、ProductVersionRaw=3.5.0.0 ✓ 全部正确。
  4. `cmake --build --preset native-local` 通过，产物 `build/bin_Release/glance3d.exe`（`f3d.vcxproj -> ...glance3d.exe`）✓；额外冒烟：glance3d.exe 无头出图 f3d.glb 400×300，Read 读图 logo 模型+UI+gizmo 正常，证明加资源后二进制未损坏 ✓。
  5. **dev-strict（F3D_STRICT_BUILD）**：US-002 只改资源(.rc)+模板+CMake，不动任何 C++ 源码；.rc 由 rc.exe 编译、不受 `/W4 /WX` 约束，**零新增 strict 告警**。dev-local 全绿仍被 pre-existing 库告警债（scene.h C4251→C2220，failure 停在 libf3d 尚未到 app/RC 阶段）阻断——与本 story 无关，详见 Codebase Patterns ⚠️ 段。生成头在 build-dev/application/ 亦已正确生成。
- **取舍**：版本注入选“生成头 + #include”而非“整份 .rc 走 configure_file”，是为了保持 `logo.ico` 相对路径不动（避免 configure 到 binary dir 后图标相对引用/正斜杠转义的坑），且 acceptanceCriteria 也是“在 resources/f3d.rc 补 VERSIONINFO 块”。RC 找生成头依赖 target 的 build-tree include 目录（VS 生成器自动传给 ResourceCompile），已两树实证。
- **Learnings for future iterations：** 见 Codebase Patterns 新增“Windows 版本资源（VERSIONINFO）注入范式”段。要点：改 configure_file/模板后必须重跑 cmake --preset 再 build；.rc 归 rc.exe 编、不吃 strict；VS 生成器把 target include 目录传给资源编译器故生成头可被 #include。
---

## 2026-07-10 07:00 - US-003 周边引用与帮助文本同步 glance3d
- **实现**：把缩略图扩展、Linux 桌面集成、shell 补全、man 引用的程序名从 f3d 同步到 glance3d（帮助文本走 argv[0] 已自动生效，无需改）。
  - `winshellext/F3DThumbnailProvider.cxx:103`：定位渲染进程的同目录字面量 `L"f3d.exe"`→`L"glance3d.exe"`（上方注释同步为 Glance3D）。
  - `resources/glance3d.desktop`：`TryExec=glance3d`、`Exec=glance3d %F`、`Icon=glance3d`。
  - `application/CMakeLists.txt`：man 输出 `f3d.1`→`glance3d.1`（`MAN_OUTPUT_FILE` 与 `.gz` install 两处）；Linux 图标 `RENAME "f3d.png"`×6→`glance3d.png`、`RENAME "f3d.svg"`×2→`glance3d.svg`；shell 补全 `RENAME "f3d"`→`glance3d`、`"_f3d"`→`_glance3d`、`"f3d.fish"`→`glance3d.fish`。
- **文件变更**：`winshellext/F3DThumbnailProvider.cxx`、`resources/glance3d.desktop`、`application/CMakeLists.txt`（+ `ralph/prd.json`、`ralph/progress.txt`）。
- **验证证据（逐条对齐 acceptanceCriteria）**：
  1. winshellext 字面量已改 glance3d.exe ✓（native-local 重新编译该 TU → `build/bin_Release/F3DShellExtension.dll`，无错；`F3D_WINDOWS_BUILD_SHELL_THUMBNAILS_EXTENSION` 在 WIN32 默认 ON，故本机真编到，非仅评审）。
  2. glance3d.desktop 三字段改 glance3d ✓。
  3. CMakeLists 图标/补全/man 全部改名 ✓；自洽性核对：Icon=glance3d ↔ 图标 RENAME glance3d.png/svg、Exec=glance3d ↔ US-001 OUTPUT_NAME glance3d、bash 补全 RENAME glance3d ↔ 命令名 glance3d，均一致。
  4. `glance3d.exe --help` 实测 Usage 行显示 `glance3d.exe [OPTIONS...]`（取 argv[0]，无硬编码，无需改代码）✓。
  5. `rg -i 'f3d.exe'` 全仓复查：功能性代码/配置命中 0 ✓；剩余 7 文件皆为 doc/ 使用文档（US-004 处理）+ `ralph/*`、`tasks/*`（描述本次更名的过程/需求文档，须引用旧名，性质等同历史 changelog）。
  6. `cmake --build --preset native-local` 通过（glance3d.exe + F3DShellExtension.dll 均产出）✓；额外无回归冒烟：改动后无头出图 f3d.glb 800×600，Read 读图 logo 模型+地面网格+gizmo+UI 正常。**dev-strict**：US-003 唯一 C++ 改动在 winshellext（F3DShellExtension 目标），该目标**不接受** /W4 /WX（`f3d_compile_options_private` 仅施加于 vtkext/libf3d/application/plugins，winshellext 的 CMakeLists 未引用它）→ 本 story C++ 改动天然不受 strict 约束且 Release 已零错编过；dev-local 全绿仍被 pre-existing libf3d C4251 债阻断，与本 story 无关。
- **取舍/边界**：Linux 桌面集成（.desktop/图标/补全/man）与 winshellext 注册在本机不装机、不注册 shell 扩展，按“编译期 + 命名自洽性”验收（与 tasks PRD 的验收口径一致）；winshellext .cxx 已真编到，非仅评审。
- **Learnings for future iterations：** 见 Codebase Patterns 新增两段——“哪些目标吃 F3D_STRICT_BUILD”（winshellext 不吃 /W4 /WX，改它只需 Release 编过）与“更名后的 Linux 桌面集成命名自洽”（.desktop Exec/Icon ↔ 二进制名/图标 RENAME ↔ 补全名 ↔ man 名 ↔ winshellext 字面量，改一处须同步全组）。
---

## 2026-07-10 07:15 - US-004 仓库文档与 CLAUDE.md 更新为 glance3d.exe
- **实现**：把仓库文档中的可执行文件路径 `f3d.exe`/`f3d-console.exe` 更新为 `glance3d.exe`/`glance3d-console.exe`（纯文档 story，无源码改动）。
  - `CLAUDE.md`（**被 .gitignore:43 忽略，改动在磁盘生效但不入 commit**）三处 `build/bin_Release/f3d.exe`→`glance3d.exe`：前提段、无头出图范式（`glance3d.exe testing/data/f3d.glb --output ...`）、交互回放范式（`glance3d.exe testing/data/<data> --no-config ...`），并删去已过时的括注“注意文件名仍是 f3d.exe”。
  - `doc/user/13-LIMITATIONS_AND_TROUBLESHOOTING.md`：Mesa 同目录 `f3d.exe`→`glance3d.exe`、`run f3d.exe`→`glance3d.exe`、subsystem 段 `f3d.exe --version`→`glance3d.exe`、`Use f3d-console.exe`→`glance3d-console.exe`（US-001 已把 console 目标 OUTPUT_NAME 改 glance3d-console，同步）。
  - `doc/dev/04-GETTING_STARTED.md`：VS 目标 `f3d.exe (bin\f3d.exe)`→`glance3d.exe (bin\glance3d.exe)`、file-manager 双击 `bin_Debug\f3d.exe`→`glance3d.exe`（Debug 产物同名 glance3d.exe；路径里 `C:\dev\f3d\` 检出目录名保留，非二进制）。
  - `doc/dev/15-COMMON_COMMANDS.zh-CN.md`：产物注释 `bin\Release\f3d.exe（或 bin\f3d.exe）`→`glance3d.exe`。
- **文件变更**：`CLAUDE.md`(gitignored)、`doc/user/13-LIMITATIONS_AND_TROUBLESHOOTING.md`、`doc/dev/04-GETTING_STARTED.md`、`doc/dev/15-COMMON_COMMANDS.zh-CN.md`（+ `ralph/prd.json`、`ralph/progress.txt`）。
- **验证证据（逐条对齐 acceptanceCriteria）**：
  1. CLAUDE.md 三处 `build/bin_Release/f3d.exe`→`glance3d.exe` ✓（在磁盘更新；因 gitignore 不入 diff，靠内容+实测命令验收）。
  2. 用更新后的 CLAUDE.md 无头出图命令 `build/bin_Release/glance3d.exe testing/data/f3d.glb --output shot.png --resolution 800,600 -x -g` 原样执行 → exit=0、生成 shot.png，Read 读图：Glance3D logo 模型（蓝+金）+ 地面网格 + 坐标轴 gizmo + UI 汉堡按钮全部正常 ✓；验证后删临时图。
  3. doc/user/13、doc/dev/04、doc/dev/15 中 f3d.exe 引用全部更新 ✓（含 doc/13 的 f3d-console.exe 同步）。
  4. `rg 'bin_Release/f3d\.exe'` 全仓：功能性代码/用户文档命中 0 ✓；剩余 5 命中全在 `ralph/prd.json`、`ralph/progress.txt`、`tasks/prd-desktop-rename-and-window-state.md`——描述本更名任务的 PRD/进度/需求文档，须引用旧名（性质等同历史 changelog，与 US-003 已确立的过程文档例外一致）。
  5. 纯文档改动，增量 `cmake --build --preset native-local` exit=0（无重编、产物 glance3d.exe）✓。US-004 未改源码，构建门槛仅 native-local，无需 dev-strict。
- **取舍/边界**：`doc/user/06-CONFIGURATION_FILE.md`（`f3d-console.exe --config`）与 `.github/ISSUE_TEMPLATE/bug_report.md`（`f3d-console.exe --version`）含 f3d-console.exe **及相邻裸命令名 `f3d`**，两文件均不在 US-004 criteria#3 列举的三文档内；仅改其中 f3d-console.exe 会与相邻裸 `f3d` 形成半更名不一致句 → 属更大范围的“裸命令名 f3d→glance3d”改造（当前无 story 覆盖），故本 story 不触碰、如实记为残留。doc/user/06 亦在 US-009 文档同步清单（US-009 会因配置目录路径再访该文件）。
- **Learnings for future iterations：** 见 Codebase Patterns 新增“文档更名 story 的范围与 CLAUDE.md gitignore”段——CLAUDE.md 被 gitignore（不入 diff，靠实测验收）；更名 story 只碰可执行文件名不碰裸命令名 f3d；`rg` 的“命中 0”按功能/用户文档口径理解，process 文档保留旧名是预期。
---

## 2026-07-10 07:25 - US-009 配置与缓存目录品牌化隔离（迁移式）
- **实现**：桌面用户配置目录 `f3d`→`Glance3D`（含一次性迁移），缓存目录经应用层覆盖为 `Glance3D\cache`，文档同步。
  - `application/F3DSystemTools.cxx`：`GetUserConfigFileDirectory()` 改造——抽出平台 base（`GetUserConfigBaseDirectory()`），新增匿名 namespace 的 `MigrateConfigTree()`（递归拷贝，返回普通文件数；单个不可读文件 skip+WARN，建目录失败/`no_space_on_device` throw 供上层清理）+ `ResolveUserConfigFileDirectory()`（`is_directory` 判定：新在→existing / 旧不在→fresh / 旧在新无→迁移，失败清残缺新目录+WARN+回退旧目录）。getter 用 `static const` memoize，迁移与原因码日志每进程一次。目录名 `f3d`→`Glance3D`（Win `%APPDATA%\Glance3D`、Linux `$XDG_CONFIG_HOME/Glance3D`|`~/.config/Glance3D`、macOS `~/Library/Application Support/Glance3D`），colormaps 派生路径随之生效。
  - `application/F3DStarter.cxx`：引擎创建后（NoRender+render 两支通吃，`setOptions` 前）`setCachePath(GetUserCacheDirectory()/"cache")` → `%LOCALAPPDATA%\Glance3D\cache`，try/catch `cache_exception` 仅 WARN 不致命；`engine.cxx:95` libf3d 默认不动。
  - 文档：doc/user/03-OPTIONS.md（HDRI 缓存→Glance3D/cache）、06-CONFIGURATION_FILE.md（用户 config 目录 f3d→Glance3D，系统/install `/etc/f3d`、`share/f3d` 不动）、09-COLOR_MAPS.md（用户 colormaps 目录同上）、11-DESKTOP_INTEGRATION.md（thumbnail.json 用户路径）。
- **文件变更**：`application/F3DSystemTools.cxx`、`application/F3DStarter.cxx`、`doc/user/03-OPTIONS.md`、`doc/user/06-CONFIGURATION_FILE.md`、`doc/user/09-COLOR_MAPS.md`、`doc/user/11-DESKTOP_INTEGRATION.md`（+ `ralph/prd.json`、`ralph/progress.txt`）。
- **验证证据（逐条对齐 acceptanceCriteria，全部真 %APPDATA% 端到端 + 事后清理）**：
  1. 目录名改 Glance3D ✓（日志与 FS 均见 `…\Roaming\Glance3D`）。
  2. 一次性迁移递归含子目录 ✓：旧 f3d 放 root 标记 + colormaps 子目录标记 → 启动 → 日志 `config-dir: migrated(2 files) …\f3d -> …\Glance3D`，新目录 root+colormaps 标记均在，旧目录文件数不变(2=2)、标记仍在。
  3+4. 四原因码全实测 ✓：`fresh`（都不存在，不建目录，仅返回路径）/ `existing`（新目录在→直接用，往旧目录新加文件**不**再拷）/ `migrated(<n> files)` / `migrate-failed-fallback`（在 `%APPDATA%\Glance3D` 放普通文件强制建目录失败 → WARN 原因码 + 残缺新目录被 `remove_all` 清掉 + 回退旧目录；旧目录完好 3=3；下次启动自动 `migrated(3 files)` 重试成功）。unreadable-file skip 为逐条 WARN 的防御代码，Windows 下 ACL 注入单文件失败较繁琐，按代码走查确认（DoD 诚实边界）。
  5. 缓存隔离 ✓：日志 `cache-dir: …\Local\Glance3D\cache`，FS 确认该目录被创建；engine.cxx:95 未改。
  6. 端到端已如上，全部 `%APPDATA%` 真目录 + 唯一标记 + 事后清理（收尾把 `%APPDATA%\f3d`、`%APPDATA%\Glance3D` 删回“都不存在”的初始态）✓。
  7. 四文档路径更新 ✓（仅改用户目录 f3d→Glance3D，`/etc/f3d`、`/usr/share/f3d`、`[install_dir]/share/f3d` 等系统/安装路径保留 f3d——非本 story 语义）。
  8. `--no-config` 免疫 ✓：植入旧 f3d 目录后 `--no-config` 启动，日志 0 条 `config-dir:`、`%APPDATA%\Glance3D` 未创建（迁移不触发）。
  9. `cmake --build --preset native-local` 通过、产物 glance3d.exe ✓；**dev-strict**：用「隔离式单文件 /W4 /WX」验（见 Codebase Patterns 新增段）——`F3DSystemTools.cxx` 直接 EXITCODE=0 零告警；`F3DStarter.cxx` 在临时中和 scene.h C4251 债后 EXITCODE=0 零告警（未中和前唯一告警就是 pre-existing scene.h C4251，非本 story）。两文件 strict 干净、零新增告警。库债 scene.h/scene_impl.cxx 已 `git checkout` 还原、build-dev 临时 F3DIcon.h 已删。
- **无回归**：静态出图 f3d.glb 400×300 Read 读图（logo+网格+gizmo+UI 全对）；交互回放 `TestInteractionRollCameraRotation`（--no-config）--reference 误差 0.097>阈值 0.04，但**读实际图 vs 基线图肉眼完全一致**（同一相机滚转角/模型/网格/gizmo/UI），属 GPU/VTK 环境性 AA 误差（CLAUDE.md 明示“误差大≠缺陷”），且本 story --no-config 下配置代码根本不跑、缓存覆盖仅 mkdir，逻辑上不可能影响像素。`--no-render`+数据文件 exit=1（"No interactor"）经 git stash 对照证明为 **pre-existing**、非本轮引入。
- **取舍/边界**：① 迁移与 rename 合并在 getter 内并 memoize，避免每次 colormap 查找重复迁移/重复日志，且让“迁移失败本次回退旧目录”语义自洽。② `is_directory` 而非 `exists` 判定，杂散非目录文件不当作已建配置目录，也顺便让 fallback 路径可注入测试。③ 缓存覆盖对 NoRender 引擎也生效（无害、mkdir 而已），保持品牌一致。④ macOS/Linux 迁移仅代码走查（本机 Windows）。
- **Learnings for future iterations：** 见 Codebase Patterns 两处新增段——“隔离式 strict 验证：单文件 /W4 /WX 编译”（未来改 application 源码的 story 复用：单文件 ClCompile、缺 F3DIcon.h 从 Release 树拷、include scene.h 的文件临时中和 C4251 债隔离告警）与“用户配置目录品牌隔离 + 迁移”（memoize getter、原因码、SHGetKnownFolderPath 不认 APPDATA 重定向、缓存走应用层覆盖）。
---

## 2026-07-10 07:55 - US-005 共享核心窗口几何读取 API（getPosition/最大化）
- **实现**：给 `f3d::window` 增补两个 additive 只读几何 API，为后续几何持久化（US-006/007）打底。
  - `library/public/window.h`：加 `#include <array>`；新增 `[[nodiscard]] virtual std::array<int,2> getPosition() const = 0`（返回 `{x,y}`，与 `setPosition` 同 top-left 屏幕坐标约定）与 `[[nodiscard]] virtual bool isMaximized() const = 0`（Windows-only 语义）；头注写明 offscreen/mock(NONE)/WASM 返回 last-set 或 `{0,0}`、非 Windows/无 HWND 恒 false、均不抛。
  - `library/private/window_impl.h`：加两个 `override` 声明。
  - `library/src/window_impl.cxx`：`getPosition()` = `RenWin->GetPosition()` 直读，Cocoa 分支镜像 `setPosition` 的 Y 翻转以 round-trip；`isMaximized()` = `#ifdef _WIN32` 下 `static_cast<HWND>(RenWin->GetGenericWindowId())` → `GetWindowPlacement` → `showCmd == static_cast<UINT>(SW_SHOWMAXIMIZED)`，HWND 空/失败/非 Windows 返回 false。
  - `application/F3DStarter.cxx`：`ApplyPositionAndResolution` 里显式 `--position` 应用后加 DEBUG 日志读回 `getPosition()`+`isMaximized()`，作 round-trip 运行时证据。
- **文件变更**：`library/public/window.h`、`library/private/window_impl.h`、`library/src/window_impl.cxx`、`application/F3DStarter.cxx`（+ `ralph/prd.json`、`ralph/progress.txt`）。
- **验证证据（逐条对齐 acceptanceCriteria）**：
  1. window.h 加 getPosition()/isMaximized()，Windows 走 GetGenericWindowId 的 HWND + GetWindowPlacement，平台宏守卫、非 Windows 返回 false ✓。
  2. 无窗口/无头/WASM 安全语义（返回 last-set 或 0,0、不崩）头注写明 + 实证（offscreen 不崩、读回 last-set）✓。
  3. **round-trip 三方对齐** ✓：真窗（`--interaction-test-play=…/TestInteraction2DZoom.log` 不带 --output → onscreen）`--position 250,175` → 日志 `[DEBUG] Window geometry: requested position (250, 175), window reports (250, 175), maximized=false`；PowerShell P/Invoke `GetWindowRect` 实测 Left=250 Top=175、`IsZoomed`=False，全一致；用完 `Stop-Process` 关窗。offscreen（--output --position 300,200）亦读回 (300,200)/false 不崩、出图 Read 读图 logo 正常。
  4. **两端回归** ✓：`cmake --build --preset native-local` 通过（产物 glance3d.exe）；`npm run build`（wasm threads，`F3D_STRICT_BUILD=ON`）重编 `window_impl.cxx.o`（日志 [6/10]）+ 链接 `bin\f3d.js` + copy dist，exit 0，仅 pre-existing pthread+memory-growth em++ 告警（无关）——共享核心 clang 端 strict 也干净。
  5. js/python 绑定未暴露新 API（additive 非破坏），零绑定改动 ✓。
  6. `native-local` 通过 ✓；**dev-strict** 用隔离式单文件 /W4 /WX（Codebase Patterns 段）：临时中和 scene.h 4 DTO C4251 债后，`window_impl.cxx` 与 `F3DStarter.cxx` 各 `EXITCODE=0` 零新增告警，编完 `git checkout -- library/public/scene.h` 还原、删 build-dev 临时 F3DIcon.h ✓。
- **取舍/边界**：① 只在 window_impl 一处实现（window 唯一具体子类）→ 两端受益。② `isMaximized` 非 Windows 恒 false 为设计降级（Linux/macOS best-effort，Wayland 位置天然不可恢复，交由 US-007 记录）。③ Cocoa getPosition Y 翻转分支仅代码走查（本机 Windows）。④ round-trip debug 日志放 app 层 `ApplyPositionAndResolution`（US-006/007 也用得上），非库层 setPosition（避免 SDK 消费者每次 setPosition 都打日志）。⑤ 未加 SDK ctest 断言（BUILD_TESTING 在本机被 pre-existing 库债/Debug-VTK 阻断、无法运行验证，不落地无法自动验证的测试断言以免为他人引入 flaky 用例）。
- **Learnings for future iterations：** 见 Codebase Patterns 两处新增段——“共享核心窗口几何读取 API”（window_impl 唯一子类、Cocoa Y 翻转、SW_SHOWMAXIMIZED 须 cast UINT 避 C4389、windows.h 经 VTK 传递、round-trip 真窗抓 rect 技巧）与“wasm 构建 emsdk 自激活失效”（显式 EMSDK/EM_CONFIG/PATH、管道尾 exit code 掩盖 npm 失败须读输出、wasm strict 顺带校验共享核心）。
---
