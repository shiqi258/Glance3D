{
  "project": "Glance3D",
  "branchName": "ralph/desktop-rename-and-window-state",
  "description": "桌面应用可执行文件更名为 glance3d、启动窗口几何（首启居中默认值/跨会话记忆/根治加载后重置）、配置与缓存目录品牌化隔离。完整需求、FR 编号与设计决策见 tasks/prd-desktop-rename-and-window-state.md（已评审确认）。本项目为 C++ 桌面应用：每个 story 的构建门槛 = cmake --build --preset native-local 通过 + dev 预设严格编译（F3D_STRICT_BUILD）通过；UI/行为验证一律用仓库自动化手段（无头出图+Read 读图、%LOCALAPPDATA%\\Glance3D\\logs 日志断言、testing/recordings 回放 --reference 比对、PowerShell P/Invoke 查询真实窗口 rect），禁止要求人工操作。纪律：只本地提交不 push；改动经验证无效即回退；不得为通过比对而重生成 testing/baselines 基线图。",
  "userStories": [
    {
      "id": "US-001",
      "title": "构建产物更名为 glance3d",
      "description": "作为用户，我希望本项目桌面程序的文件名是 glance3d.exe，从而在任务管理器、开始菜单、脚本与自动化中不再和其它已安装的 f3d.exe 混淆。",
      "acceptanceCriteria": [
        "application/CMakeLists.txt：CMake 目标名 f3d / f3d-console 保持不变，仅设 OUTPUT_NAME 为 glance3d / glance3d-console（保证 $<TARGET_FILE:f3d> 与 f3dTargets 导出零波及）",
        "cmake --build --preset native-local 后产物为 build/bin_Release/glance3d.exe",
        "删除构建树残留的旧 build/bin_Release/f3d.exe，防止继续误用旧产物",
        "build/bin_Release/glance3d.exe testing/data/f3d.glb --output shot.png --resolution 800,600 出图成功且 Read 读图内容正常（运行时资源按 <exe>/../share/f3d 目录锚点解析，须实证不受文件名影响），验证后删除临时图",
        "全局排查 F3DSystemTools 等处按 argv[0]/exe 文件名推导路径的逻辑，确认无被更名破坏之处",
        "构建通过：cmake --build --preset native-local；dev 预设严格编译（F3D_STRICT_BUILD）通过"
      ],
      "priority": 1,
      "passes": true,
      "notes": "仅改 application/CMakeLists.txt：f3d/f3d-console 目标各设 OUTPUT_NAME=glance3d/glance3d-console，目标名与 f3dTargets 导出不变。native-local 构建通过，产物 build/bin_Release/glance3d.exe，无旧 f3d.exe 残留。无头出图 glance3d.exe testing/data/f3d.glb --output --resolution 800,600 -x -g Read 读图正常（logo 模型+网格+gizmo+UI 全对），证明资源按 <exe>/../../share/f3d 目录锚点解析、与文件名无关。argv[0] 全局排查：唯一用处是 F3DOptionsTools.cxx:277 帮助显示名（非路径），路径解析全走 GetModuleFileNameW/GetApplicationPath（文件名无关）；--help Usage 行已自动显示 glance3d.exe。f3d-console 目标受 F3D_WINDOWS_BUILD_CONSOLE_APPLICATION 守卫、本预设未启用，改动正确但未由 native-local 触发。dev-strict 说明：US-001 未改 C++ 源码，不引入任何新 strict 告警；dev-local 全绿在本机不可达（pre-existing 库告警债 scene.h C4251 + scene_impl.cxx C4702… 与环境性 Release-VTK/Debug-CRT 测试链接 LNK2038，均与更名无关），详见 ralph/progress.txt 的 Codebase Patterns ⚠️ 段——留给源码型 story（US-005+）前置治理。"
    },
    {
      "id": "US-002",
      "title": "Windows 版本信息资源（VERSIONINFO）",
      "description": "作为用户，我希望在任务管理器/文件属性里看到 Glance3D 的产品名与版本号，而不是无元数据的裸 exe。",
      "acceptanceCriteria": [
        "resources/f3d.rc（现仅一行图标）补充 VERSIONINFO 块：ProductName=Glance3D、FileDescription=Glance3D - A fast and minimalist 3D viewer、OriginalFilename=glance3d.exe",
        "FileVersion/ProductVersion 取自 F3D_VERSION（经 configure_file 或等价机制注入，不写死版本号）",
        "PowerShell 验证：(Get-Item build/bin_Release/glance3d.exe).VersionInfo 显示上述字段与正确版本号",
        "构建通过：cmake --build --preset native-local；dev 预设严格编译（F3D_STRICT_BUILD）通过"
      ],
      "priority": 2,
      "passes": true,
      "notes": "新增 resources/Glance3DVersion.h.in（configure_file 模板，占位 @F3D_MAJOR/MINOR/PATCH_VERSION@ 与 \"@F3D_VERSION@\"，版本单一来源自顶层 CMakeLists 不写死）；resources/f3d.rc 保留首行图标、新增 #include <windows.h> + #include \"Glance3DVersion.h\" 与 VS_VERSION_INFO VERSIONINFO 块（FILEVERSION/PRODUCTVERSION=GLANCE3D_VERSION_MAJOR,MINOR,PATCH,0；StringFileInfo 040904b0：ProductName=Glance3D、FileDescription=Glance3D - A fast and minimalist 3D viewer、OriginalFilename=glance3d.exe、FileVersion/ProductVersion=GLANCE3D_VERSION_STR、附 CompanyName/InternalName/LegalCopyright；VarFileInfo Translation 0x409,1200）；application/CMakeLists.txt 在 F3DConfig.h 之后加 WIN32 守卫的 configure_file(Glance3DVersion.h.in → ${CMAKE_CURRENT_BINARY_DIR}/Glance3DVersion.h @ONLY)。native-local 构建通过、产物 build/bin_Release/glance3d.exe；PowerShell (Get-Item ...).VersionInfo 实测 ProductName=Glance3D / FileDescription=Glance3D - A fast and minimalist 3D viewer / OriginalFilename=glance3d.exe / FileVersion=3.5.0 / ProductVersion=3.5.0 / FileVersionRaw=3.5.0.0 全部正确；加资源后 glance3d.exe 无头出图正常（二进制未损坏）。RC 找生成头靠 f3d_create_exec() 里 target 的 build-tree include 目录（VS 生成器传给 ResourceCompile），native-local/dev-local 两树均已生成头且 Release 树 RC 编译通过。dev-strict：只改资源(.rc)+模板+CMake、不动 C++ 源码，.rc 由 rc.exe 编译不受 /W4 /WX 约束 → 零新增 strict 告警；dev-local 全绿仍被 pre-existing 库告警债（scene.h C4251→C2220，停在 libf3d 未到 app/RC）阻断，与本 story 无关（详见 progress.txt Codebase Patterns ⚠️ 段）。改 configure_file/模板后须重跑 cmake --preset 再 build。"
    },
    {
      "id": "US-003",
      "title": "周边引用与帮助文本同步 glance3d",
      "description": "作为用户/集成方，我希望缩略图扩展、Linux 桌面集成、shell 补全、帮助文本引用的程序名与新产物一致，不留指向失效的 f3d 引用。",
      "acceptanceCriteria": [
        "winshellext/F3DThumbnailProvider.cxx 约 103 行：定位可执行文件的字面量 L\"f3d.exe\" 改为 L\"glance3d.exe\"",
        "resources/glance3d.desktop：TryExec=glance3d、Exec=glance3d %F、Icon=glance3d",
        "application/CMakeLists.txt：Linux 图标安装 RENAME 由 f3d.png/f3d.svg 改为 glance3d.png/glance3d.svg；shell 补全安装名 f3d/_f3d/f3d.fish 改为 glance3d/_glance3d/glance3d.fish；man 生成 f3d.1 改为 glance3d.1",
        "build/bin_Release/glance3d.exe --help 的 Usage 行显示 glance3d（若硬编码则修改，若取 argv[0] 则实测确认输出）",
        "rg -i 'f3d\\.exe' 全仓复查：除 doc/ 使用文档（US-004 处理）与历史性 changelog 外命中 0",
        "构建通过：cmake --build --preset native-local；dev 预设严格编译（F3D_STRICT_BUILD）通过"
      ],
      "priority": 3,
      "passes": true,
      "notes": "三处改动全部落定：(1) winshellext/F3DThumbnailProvider.cxx:103 定位字面量 L\"f3d.exe\"→L\"glance3d.exe\"（含上方注释同步 Glance3D），缩略图 DLL 按 glance3d.exe 定位渲染进程；native-local 已编译该 TU→F3DShellExtension.dll，无错。(2) resources/glance3d.desktop：TryExec=glance3d / Exec=glance3d %F / Icon=glance3d。(3) application/CMakeLists.txt：man 输出 f3d.1→glance3d.1（MAN_OUTPUT_FILE 与 .gz install 两处）；Linux 图标 RENAME f3d.png×6→glance3d.png、f3d.svg×2→glance3d.svg；shell 补全 RENAME f3d→glance3d / _f3d→_glance3d / f3d.fish→glance3d.fish。自洽性：desktop Icon=glance3d ↔ 图标 RENAME glance3d.png/svg；Exec=glance3d ↔ US-001 OUTPUT_NAME glance3d；bash 补全 RENAME glance3d ↔ 命令名 glance3d，均一致。--help Usage 行实测显示 glance3d.exe（取 argv[0]，无需改代码）。cmake --build --preset native-local 通过（glance3d.exe + F3DShellExtension.dll 均出），加改动后无头出图 f3d.glb 800×600 Read 读图正常（logo+网格+gizmo+UI 全对，无回归）。rg -i 'f3d.exe' 全仓复查：功能性代码/配置命中 0；剩余仅 doc/ 使用文档（US-004 处理）与 ralph/*、tasks/*（描述本次更名的过程/需求文档，性质等同历史 changelog，须引用旧名）。dev-strict：US-003 唯一 C++ 改动在 winshellext（F3DShellExtension 目标），该目标不接受 /W4 /WX——f3d_compile_options_private(=/W4 /WX) 仅施加于 vtkext/libf3d/application/plugins（见 CMakeLists 及各子 CMakeLists），winshellext 的 CMakeLists 未引用它，故本 story C++ 改动天然不受 strict 约束且 Release 已编译零错；dev-local 全绿仍被 pre-existing libf3d scene.h C4251 债阻断，与本 story 无关（详见 progress.txt Codebase Patterns ⚠️ 段）。"
    },
    {
      "id": "US-004",
      "title": "仓库文档与 CLAUDE.md 更新为 glance3d.exe",
      "description": "作为后续维护者（人或 AI），我希望文档里的可执行路径全部指向新产物，无人值守流程照抄可用。",
      "acceptanceCriteria": [
        "CLAUDE.md 中所有 build/bin_Release/f3d.exe（无头出图、交互回放范式等）更新为 glance3d.exe",
        "实测 CLAUDE.md 文档中的无头出图命令可运行出图（用更新后的路径原样执行一次）",
        "doc/user/13-LIMITATIONS_AND_TROUBLESHOOTING.md、doc/dev/04-GETTING_STARTED.md、doc/dev/15-COMMON_COMMANDS.zh-CN.md 中的 f3d.exe 引用全部更新",
        "rg 'bin_Release/f3d\\.exe' 全仓命中 0",
        "纯文档改动：cmake --build --preset native-local 增量构建仍通过（应为无操作）"
      ],
      "priority": 4,
      "passes": true,
      "notes": "四文档路径更新落定：(1) CLAUDE.md（.gitignore:43 忽略，为本机工作文件，改动在磁盘上、不入 commit）三处 build/bin_Release/f3d.exe→glance3d.exe（前提段、无头出图范式 118 行、交互回放范式 150 行），并删去已过时的『注意文件名仍是 f3d.exe』括注。(2) doc/user/13-LIMITATIONS_AND_TROUBLESHOOTING.md：Mesa 同目录 f3d.exe→glance3d.exe、run f3d.exe→glance3d.exe、subsystem 段 f3d.exe --version→glance3d.exe、Use f3d-console.exe→glance3d-console.exe（US-001 已把 console 目标 OUTPUT_NAME 改 glance3d-console，同步）。(3) doc/dev/04-GETTING_STARTED.md：VS 目标 f3d.exe (bin\\f3d.exe)→glance3d.exe (bin\\glance3d.exe)、file-manager 双击 bin_Debug\\f3d.exe→glance3d.exe（Debug 配置产物同名 glance3d.exe；路径中的 C:\\dev\\f3d\\ 检出目录名保留，非二进制）。(4) doc/dev/15-COMMON_COMMANDS.zh-CN.md：产物注释 bin\\Release\\f3d.exe（或 bin\\f3d.exe）→glance3d.exe。验证：criteria#2 用更新后的 CLAUDE.md 命令 `glance3d.exe testing/data/f3d.glb --output shot.png --resolution 800,600 -x -g` 原样执行 exit=0、出图 Read 读图正常（logo 模型+网格+gizmo+UI 全对）后删临时图；criteria#4 `rg 'bin_Release/f3d\\.exe'` 剩余命中仅 ralph/*、tasks/*（描述本更名任务的 PRD/进度/需求文档，性质等同历史 changelog 须引用旧名，与 US-003 已确立的过程文档例外一致），功能/用户文档命中 0；criteria#5 增量 `cmake --build --preset native-local` exit=0（纯文档改动、无重编，产物 glance3d.exe）。US-004 未改任何源码，构建门槛仅 native-local（无需 dev-strict）。边界/残留：doc/user/06-CONFIGURATION_FILE.md 与 .github/ISSUE_TEMPLATE/bug_report.md 内含 `f3d-console.exe` 及裸命令名 `f3d`（如 `f3d --version`），不在 US-004 criteria#3 列举的三文档内；仅改其中 f3d-console.exe 会与相邻裸 `f3d` 形成半更名的不一致句，属更大范围的『裸命令名 f3d→glance3d』改造，当前无 story 覆盖，故不在本 story 触碰、如实记为残留（doc/user/06 亦在 US-009 文档同步清单，US-009 会因配置目录路径再访该文件）。"
    },
    {
      "id": "US-009",
      "title": "配置与缓存目录品牌化隔离（迁移式）",
      "description": "作为用户，我希望本项目的配置目录、缓存目录与机器上官方 F3D 的分开，避免两个程序读写同一份配置/缓存互相干扰。",
      "acceptanceCriteria": [
        "application/F3DSystemTools.cxx 约 139 行 GetUserConfigFileDirectory()：目录名 f3d 改为 Glance3D（Windows %APPDATA%\\Glance3D、Linux $XDG_CONFIG_HOME/Glance3D 无则 ~/.config/Glance3D、macOS ~/Library/Application Support/Glance3D）；colormaps 等派生查找路径随之生效",
        "一次性迁移：新目录不存在且旧 …\\f3d 存在时递归拷贝（含 colormaps/ 等子目录）到新目录；绝不移动/删除/修改旧目录（它仍是官方 F3D 的活动目录）",
        "新目录已存在则直接使用、不再读旧目录；拷贝中途失败（权限/磁盘满）则清理残缺新目录 + WARN 日志 + 本次回退读旧目录、下次启动自动重试；不可读文件跳过并逐条记录；任何情况不崩溃",
        "迁移决策原因码写日志：config-dir: fresh | migrated(<n> files) | existing | migrate-failed-fallback",
        "缓存隔离（SHOULD）：应用层经 engine::setCachePath 覆盖为 %LOCALAPPDATA%\\Glance3D\\cache（各平台等价缓存根）；library/src/engine.cxx 约 95 行的 libf3d 默认值不动；旧缓存不迁移不删除",
        "端到端自动验证（Windows，真实 %APPDATA%，带唯一标记的测试文件+事后清理；注意 SHGetKnownFolderPath 不受 APPDATA 环境变量重定向影响）：旧目录放标记文件 → 启动 → 断言新目录出现拷贝、旧目录原样、日志原因码正确",
        "文档同步：doc/user/06-CONFIGURATION_FILE.md、doc/user/09-COLOR_MAPS.md、doc/user/11-DESKTOP_INTEGRATION.md、doc/user/03-OPTIONS.md 中的 f3d 用户目录路径全部更新",
        "--no-config 行为照旧（既有测试免疫）",
        "构建通过：cmake --build --preset native-local；dev 预设严格编译（F3D_STRICT_BUILD）通过"
      ],
      "priority": 5,
      "passes": true,
      "notes": "application/F3DSystemTools.cxx：GetUserConfigFileDirectory() 目录名 f3d→Glance3D（Win %APPDATA%\\Glance3D、Linux $XDG_CONFIG_HOME/Glance3D|~/.config/Glance3D、macOS ~/Library/Application Support/Glance3D）；抽出 GetUserConfigBaseDirectory()，新增匿名 ns 的 MigrateConfigTree()（递归拷贝返回普通文件数；单文件不可读 skip+WARN，建目录失败/no_space_on_device throw 供上层清理）+ ResolveUserConfigFileDirectory()（is_directory 判定：新在→existing/旧不在→fresh/旧在新无→迁移，失败清残缺新目录+WARN+回退旧目录）；getter 用 static const memoize，迁移与原因码日志每进程一次；colormaps（F3DColorMapTools::Find 用 getter/\"colormaps\"）派生路径自动切换。application/F3DStarter.cxx：引擎创建后（NoRender+render 两支、setOptions 前）setCachePath(GetUserCacheDirectory()/\"cache\")→%LOCALAPPDATA%\\Glance3D\\cache，try/catch cache_exception 仅 WARN 不致命；engine.cxx:95 libf3d 默认不动。文档同步 doc/user/03,06,09,11（仅用户目录 f3d→Glance3D，系统/install /etc/f3d、share/f3d 保留）。端到端真 %APPDATA% 验证（唯一标记+事后清理，机器复原到都不存在）：criteria#2 迁移含 colormaps 子目录日志 migrated(2 files)、新目录出现拷贝、旧目录原样(2=2)；criteria#3+4 四原因码全实测 fresh/existing/migrated(<n> files)/migrate-failed-fallback（在 %APPDATA%\\Glance3D 放普通文件强制建目录失败→WARN+remove_all 清残缺+回退旧目录，旧目录完好 3=3，下次启动 migrated(3 files) 自动重试）；unreadable-file skip 为逐条 WARN 防御代码、Windows ACL 注入繁琐按代码走查（DoD 诚实边界）。criteria#5 日志 cache-dir: …\\Local\\Glance3D\\cache、FS 确认创建。criteria#8 --no-config 免疫：植旧 f3d 目录后 --no-config 启动 0 条 config-dir、Glance3D 未建。构建：native-local 通过、产物 glance3d.exe；dev-strict 用隔离式单文件 /W4 /WX（见 progress.txt Codebase Patterns 新增段）——F3DSystemTools.cxx 直接 EXITCODE=0 零告警、F3DStarter.cxx 临时中和 pre-existing scene.h C4251 债后 EXITCODE=0 零告警，两文件 strict 干净零新增；库债 scene.h/scene_impl.cxx 已 git checkout 还原、临时 F3DIcon.h 已删。无回归：静态出图 Read 读图正常；TestInteractionRollCameraRotation(--no-config) --reference 0.097>阈值但读图与基线肉眼完全一致（GPU/VTK 环境性 AA 误差，--no-config 下配置代码不跑、缓存覆盖仅 mkdir，逻辑上不影响像素）；--no-render+数据文件 exit=1 经 git stash 对照证明为 pre-existing 非本轮引入。边界：macOS/Linux 迁移仅代码走查（本机 Windows）；缓存覆盖对 NoRender 也生效（无害 mkdir，保持品牌一致）。"
    },
    {
      "id": "US-005",
      "title": "共享核心窗口几何读取 API（getPosition/最大化）",
      "description": "作为应用层开发者，我需要从 f3d::window 读到当前窗口位置与最大化状态，才能实现几何持久化——现有 API（library/public/window.h）只有 setSize/getWidth/getHeight/setPosition，无 getPosition。",
      "acceptanceCriteria": [
        "library/public/window.h 增补 additive API：getPosition()（返回屏幕坐标）；Windows 提供最大化状态读取（建议 isMaximized()，在 window_impl 层经 vtkRenderWindow::GetGenericWindowId 的 HWND 走 GetWindowPlacement，平台宏守卫；非 Windows 返回 false）",
        "无窗口/无头/WASM 实现给出安全语义（返回 last-set 或 0,0，不崩溃），头文件注释写明",
        "桌面端 round-trip 验证：setPosition(x,y) 后 getPosition() 读回一致（debug 日志行为证据）",
        "两端回归：npm run build（wasm）通过——共享核心改动必须桌面+网页双端回归",
        "语言绑定（js/python）不强制暴露新 API；如顺手暴露须相应绑定测试通过",
        "构建通过：cmake --build --preset native-local；dev 预设严格编译（F3D_STRICT_BUILD）通过"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-006",
      "title": "首启默认几何：光标屏工作区居中+自适应尺寸",
      "description": "作为用户，第一次启动（或无记忆可用）时，我希望窗口出现在我正在用的显示器中央、尺寸合理，而不是左上角的固定小窗。",
      "acceptanceCriteria": [
        "交互式启动且无显式 --resolution/--position 时：目标显示器 = 光标所在显示器（取不到则主屏）；逻辑尺寸 = 该显示器工作区（扣任务栏）的 70%（宽高各自计算），clamp 到 [1000×600×DPI缩放, 工作区 100%]；位置 = 工作区居中（显示器枚举用 Win32 MonitorFromPoint/GetMonitorInfo，置于应用层平台守卫内；新代码文件按 G3D 品牌命名）",
        "无头路径（--output / --no-render）行为完全不变：默认仍 1000,600、不居中——glance3d.exe testing/data/f3d.glb --output a.png 产物为 1000×600，与改动前基线图对比一致（改动前先出一张留档）",
        "macOS 专用 hack（application/F3DStarter.cxx 约 916 行 setPosition(100,800)）由居中逻辑取代（平台宏守卫；本机无法实测 macOS，代码评审验收并在 notes 注明）",
        "启动 debug 日志输出决策链：目标显示器、工作区 rect、最终窗口 rect、原因码 default-centered",
        "Windows 加强证据：PowerShell P/Invoke GetWindowRect 读实际窗口 rect，与日志一致且中心点 = 目标显示器工作区中心 ±2px",
        "抽查 2 个 testing/recordings 回放（固定带 --no-config --resolution=300,300）--reference 误差达标，证明显式分辨率路径不受影响",
        "构建通过：cmake --build --preset native-local；dev 预设严格编译（F3D_STRICT_BUILD）通过"
      ],
      "priority": 7,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-008",
      "title": "消除加载完成后的窗口几何重置",
      "description": "作为用户，打开大文件时我会趁加载中把窗口拖到合适的位置与尺寸，我希望加载完成后窗口保持我调整的样子，而不是被程序改回默认。",
      "acceptanceCriteria": [
        "动手前考证：git log -L / 对照上游 f3d，查明 application/F3DStarter.cxx 约 1338 行（LoadFileGroup() 之后）第二次 ApplyPositionAndResolution() 的引入原因，确认 --output 无头路径对它无时序依赖后再删改；若确有依赖则改为『几何尚未成功建立时才生效』的守卫式实现",
        "初始几何建立后程序不再自动改写窗口几何；原第二次调用位置留 debug 日志（如 skip geometry re-apply: already established）作为运行时证据",
        "审计其余再加载路径（--watch 重载、load_next_file_group / load_previous_file_group、remove_file_groups、拖放新文件）均不改写几何；实测其一：加载文件 A → PowerShell P/Invoke 改窗口尺寸 → --command-script 执行 load_next_file_group → GetWindowRect 断言尺寸未被改回",
        "无头回归：默认 --output（1000×600）与显式 --resolution 800,600 的产物尺寸与改动前一致，Read 读图对比无异常",
        "既有 interaction 回放抽查 ≥3 个（含 UI 类）--reference 误差达标",
        "诚实性约束：真实『加载中途拖动』涉及被阻塞消息循环的注入时序竞态，若无法无人值守可靠复现，允许以『代码路径日志证据 + 全量回归』验收，并在 notes 如实注明验证边界，不得伪造",
        "构建通过：cmake --build --preset native-local；dev 预设严格编译（F3D_STRICT_BUILD）通过"
      ],
      "priority": 8,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-007",
      "title": "窗口几何持久化与恢复（含校验/抑制/开关）",
      "description": "作为用户，我希望程序记住我上次的窗口尺寸/位置（及最大化状态），下次启动直接还原，不必每次手动拖放。",
      "acceptanceCriteria": [
        "交互式运行中几何变更防抖（≤1s）保存 + 正常退出兜底保存；原子写（临时文件+rename）到状态文件；状态文件为带 version 字段的 JSON：{version,x,y,width,height,maximized}（物理像素）",
        "状态文件位置：Windows %LOCALAPPDATA%\\Glance3D\\window-state.json（与日志同根）、Linux $XDG_STATE_HOME/Glance3D 无则 ~/.local/state/Glance3D、macOS ~/Library/Application Support/Glance3D；实现放应用层、新代码文件按 G3D 品牌命名",
        "再次交互式启动恢复保存的 rect；maximized=true 时先按普通 rect 定位再最大化（Windows 完整支持；其它平台降级为普通几何；Wayland 位置天然不可恢复，仅尺寸+最大化生效并记日志）",
        "有效性校验：保存 rect 与当前任一显示器工作区交集 ≥100×100 且标题栏可达，尺寸超出目标工作区则 clamp；不满足回退 US-006 默认居中——伪造离屏坐标（如 x=-99999）写入状态文件后启动，日志显示 restore-invalid → default-centered",
        "抑制规则：显式 --resolution 或 --position、--output、--no-render、--no-config 任一存在 → 该次运行既不恢复也不保存（状态文件 mtime 不变为证据；判定依据是『用户是否显式提供』而非值比较，复用 CLI/config 解析层的来源信息）",
        "开关：G3D_WINDOW_STATE=0 禁用、G3D_WINDOW_STATE=<path> 重定向状态文件；损坏 JSON → WARN 日志 + 回退默认，不崩溃",
        "端到端自动验证（Windows PowerShell）：启动（G3D_WINDOW_STATE 重定向到临时路径）→ P/Invoke MoveWindow 改几何 → 正常关闭 → 断言状态文件内容 → 再启动 → 日志 restored rect 与 GetWindowRect 实测一致",
        "每个决策写 debug 日志：restored / restore-invalid / default-centered / suppressed(<flag>) / saved（含最终 rect）",
        "npm run build（wasm）不受影响；构建通过：cmake --build --preset native-local；dev 预设严格编译（F3D_STRICT_BUILD）通过"
      ],
      "priority": 9,
      "passes": false,
      "notes": ""
    }
  ]
}
